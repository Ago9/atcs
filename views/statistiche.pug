doctype html
html
    head
        title timeline
        link(rel='stylesheet', href='/stylesheets/style.css')
        script(src='https://d3js.org/d3.v5.min.js')

    body
        block content

            header(class='parallax')
                 h1= "Single visitor's stats"

            h1 Visitor <b>#{visitatori.id}</b> 
            br
            h3 part of group <b>#{visitatori.gruppo}</b> that visited in data: <b>#{visitatori.data}</b>
            h3 total time spent into the museum: #{visitatori.visitTime}

            br
            br

            h3 Hold your cursor over the points to see the details

            div(id='timeline')
    
             script(src="/javascripts/timeline.js")

            script. 
                var obj = !{JSON.stringify(posizioni)}
                drawTimeline(obj)

            br 
            h1 Presentations
            br

            lu
                each pres in presentazioni

                    li  #{pres.nome} #{pres.paused}

            br
            br

            h1 Positions

            br

            lu
                each poi in posizioni

                    li #{poi.id}  from <b>#{poi.inizio}</b> to <b>#{poi.fine}</b>
