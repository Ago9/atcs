extends layout

block content

    header(class='parallax')
        h1= "Visitors"

    form(method='POST', action='/visitatore/select')
        select(name = 'visitors')
            each vis in visitatori
                 option(value=vis.id) #{vis.id}
        
        input(type='submit', value='submit')
